"""
工具调用协议提示词。
"""

TOOLCALL_PROTOCOL = """[工具调用协议 - ACTION PROTOCOL]
当你决定使用工具时，必须使用以下严格的格式：

<status>用一句话描述你即将进行的动作（面向用户，严禁提及工具名称，如：正在检索厦门实时天气数据...）</status>
<action>
{
    "tool_name": "工具名称",
    "params": {
        "参数名": "参数值"
    }
}
</action>

[强制约束]
1. 每次响应必须包含 <status>，用于在长时间执行期间安抚用户。
2. <status> 描述必须自然、专业，严禁暴露系统内部工具函数名。
3. 每次响应仅限调用一个工具。
4. 如果目标已达成，请直接输出最终结论，严禁继续调用工具。"""